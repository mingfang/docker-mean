{
  "bind": "0.0.0.0:7946",
  "_start_join": ["cloud1:7946"],
  "leave_on_terminate": true,
  "tags":{
    "role": "web",
    "_frontend": "    acl is_www hdr(host) -i www;    use_backend www if is_www",
    "frontend": "    default_backend www",
    "backend": "backend www;    server mean cloud4:3000 check"
  }
}
